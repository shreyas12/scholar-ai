"""ScholarAI Knowledge Processing Pipeline.

Builds a knowledge representation from documents (parse → clean → chunk → …),
not just an index. See ``PLAN.md`` §5. Slice A establishes the orchestrator seam.
"""
