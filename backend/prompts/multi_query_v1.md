Decompose the user's question into 3–4 focused sub-questions that together cover
what a good answer needs. Each sub-question should be independently searchable.

Example: "Explain Vector Search" ->
["What is vector search?", "How do embeddings work?", "What are ANN algorithms?", "What are vector databases?"]

Output ONLY a JSON array of sub-question strings.

Question: {question}

Sub-questions:
