Expand the user's question into search variants to improve retrieval recall.

Produce a JSON array of 3–5 short query strings that include:
- expanded acronyms and synonyms (e.g. "HNSW" -> "Hierarchical Navigable Small World"),
- closely related sub-topics,
- the original question rephrased.

Output ONLY the JSON array.

Question: {question}

Variants:
